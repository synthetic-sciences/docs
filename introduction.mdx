---
title: "What is Atlas?"
description: "Atlas stores hypotheses, experiments, sources, and decisions in a durable, branchable graph. Researchers and AI agents share the same auditable project state."
sidebarTitle: "Introduction"
---

Atlas is a graph-based research workspace from Synthetic Sciences. Every claim, experiment, source, and decision lives in an explicit, auditable structure: a graph of research nodes, an append-only research log, indexed knowledge sources, tracked artifacts, and a compute approval boundary that keeps humans in control of what runs and what spends.

<Note>
  Looking for the terminal coding agent? That's [Synci CLI](/cli/introduction), a separate product. Atlas is the research workspace; Synci CLI is the agent you run in your shell.
</Note>

## Who Atlas is for

Atlas is built for people running research programs where losing context is expensive.

- **AI researchers** comparing methods, prompts, training runs, or evaluations across many experiments.
- **Scientists** turning papers, datasets, notebooks, and results into an audit trail they can branch and reproduce.
- **Agent builders** who need a real project memory layer rather than fuzzy chat-based RAG.
- **Teams** that want agents to plan and execute work without handing over control of budget and research direction.

## The four core pieces

Atlas combines four capabilities that normally live in separate tools.

- **[Research graph](/concepts/research-graph).** A DAG of staged and committed nodes (hypotheses, experiments, insights, branches, merges). Committed nodes form durable history; new work branches from them instead of overwriting them.
- **[Agent harness](/concepts/agent-harness).** An Atlas-native ReAct agent that hydrates from graph state, searches prior work, stages experiments, writes the research log, and spawns approved sub-agents. It does not rely on chat history as its source of truth.
- **[Knowledge layer](/concepts/knowledge-layer).** Unified search across your Atlas graph and external sources indexed through Nia (papers, repositories, documentation, datasets) from one interface.
- **[Artifact and filesystem layer](/guides/organize-research).** Node artifacts, project volume files, research reports, sub-agent working directories, asset cards, uploads, previews, export, and import. Evidence stays attached to the nodes that produced it.

## What makes Atlas different

Research state in Atlas is inspectable the same way source code is inspectable. Every graph mutation happens through a typed, auditable tool, not a hidden PM agent guessing your intent from conversation. Compute-spending sub-agents require explicit approval before they start. Committed nodes cannot be silently rewritten. The research log is append-only.

The goal is simple: make research work you can find, branch, reproduce, and hand off, months after the original work happened.

<Note>
  You can start using the Atlas web app without any additional setup. Open [atlas.syntheticsciences.ai](https://atlas.syntheticsciences.ai) and sign in.
</Note>
