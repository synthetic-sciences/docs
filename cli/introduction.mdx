---
title: "What is Synsci CLI?"
description: "Synsci CLI is a terminal coding agent for scientific work. Research modes for physics, chemistry, biology, general research, and the model flywheel for post-training."
sidebarTitle: "Introduction"
---

Synsci CLI is a terminal coding agent built for scientific work. The binary is `synsc`. It runs alongside your shell and gives you a single agent that already knows how to do science. Pick a research mode and the agent loads domain-aware tools, cloud credentials, and the right backend automatically.

```bash
npx synsci
```

The npm package is [`@synsci/cli`](https://www.npmjs.com/package/@synsci/cli). The dashboard lives at [cli.syntheticsciences.ai](https://cli.syntheticsciences.ai).

<Note>
  Synsci CLI is a separate product from [Thesis](/introduction). Thesis is the research workspace (graphs, experiments, MCP server). Synsci CLI is the agent you run in your shell. They pair via [MCP](/cli/mcp) if you use both.
</Note>

## What you get

- **Research modes** for [general research](/cli/research/research), [physics](/cli/research/physics), [chemistry](/cli/research/chemistry), [biology](/cli/research/biology), and the [model flywheel](/cli/research/flywheel) for building specialized LLMs from production data. Each mode primes the agent with domain conventions and routes calls to the right backend.
- **Bundled domain tools** for each mode. From DFT and molecular dynamics in chemistry to GRPO and vLLM in flywheel. The agent loads them automatically when your prompt calls for them.
- **[Credential sync](/cli/credentials)** for Hugging Face, Weights & Biases, Modal, Lambda, AWS, and GCP. Tokens come from your dashboard and reach subprocesses without ever touching disk.
- **[Included credits](/cli/credits)** that cover the agent's work. Pre-call balance checks block runaway loops before they bill.

## Two agents

- `build` is the default. Full filesystem and shell access for development work.
- `plan` is read-only. Same context and tools, but cannot mutate anything.

Switch with the agent toggle in the [web UI](/cli/web) header, or press <kbd>Tab</kbd> in the TUI. Mention `@general` in any message to delegate a complex search or multi-step task to a sub-agent that runs in its own context.

## Where it fits

| You want to | Reach for |
|---|---|
| Run scientific compute work in your terminal | Synsci CLI with the right [research mode](/cli/research/research) |
| Track hypotheses, experiments, and decisions over time | [Thesis](/introduction) |
| Use both, with the agent writing code and Thesis storing what was tried | Run both, paired through [MCP](/cli/mcp) |

## What's next

- **[Install Synsci CLI](/cli/installation).** `npx synsci` does the entire setup.
- **[Quickstart](/cli/quickstart).** Connect, pick a mode, send your first prompt.
- **[Web UI](/cli/web).** The recommended day-to-day interface.
- **[Research modes](/cli/research/research).** What each mode covers and how to switch.
